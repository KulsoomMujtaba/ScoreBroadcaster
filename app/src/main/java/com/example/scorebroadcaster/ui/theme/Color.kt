package com.example.scorebroadcaster.ui.theme

import androidx.compose.ui.graphics.Color

// =============================================================================
// Cricket-inspired brand palette — modern blue sports visual identity
// Primary brand: cricket blue | Surfaces: clean white | Text: dark readable
// =============================================================================

// Primary brand blues
val CricketBlue = Color(0xFF1E5EFF)            // Primary brand blue
val CricketDarkBlue = Color(0xFF153ECC)         // Primary dark blue
val CricketLightBlue = Color(0xFFE8F0FF)        // Primary light blue / container

// Backgrounds and surfaces
val CricketBackground = Color(0xFFF6F8FB)       // App background — very light blue tone
val CricketSurfaceVariant = Color(0xFFEEF2F7)   // Surface variant / subtle card fill
val CricketOutline = Color(0xFFD7DEE8)          // Dividers and borders

// Text hierarchy
val CricketTextPrimary = Color(0xFF0F172A)      // On-surface / primary dark text
val CricketTextSecondary = Color(0xFF64748B)    // On-surface-variant / secondary text

// Error / Wicket state
val CricketError = Color(0xFFD32F2F)            // Wicket / destructive action
val CricketErrorContainer = Color(0xFFFFDAD6)   // Light error container
val OnCricketError = Color(0xFFFFFFFF)
val OnCricketErrorContainer = Color(0xFF410002)

// Cricket semantic accents — boundary buttons and chips
// Four (4): primary blue emphasis
val BoundaryFourContainer = Color(0xFF1E5EFF)   // Primary blue container for 4 buttons/chips
val OnBoundaryFourContainer = Color(0xFFFFFFFF) // White text on four container

// Six (6): darker blue emphasis
val BoundarySixContainer = Color(0xFF0B3FB3)    // Dark blue container for 6 buttons/chips
val OnBoundarySixContainer = Color(0xFFFFFFFF)  // White text on six container

// Extras: warm amber accent
val ExtrasAccentContainer = Color(0xFFFEF3C7)   // Light amber for extras chips
val OnExtrasAccentContainer = Color(0xFF92400E) // Dark text on extras

// Secondary brand colour (blue accent mid-tone — M3 secondary role)
val BlueAccentMid = Color(0xFF4B7BFF)           // Blue secondary mid-tone

// Extras amber (M3 tertiary role)
val ExtrasAmber = Color(0xFFF59E0B)             // Warm amber for extras